# terraspectra-infotact
Traditional satellite crop monitoring uses standard RGB or simple infrared imagery. These models only detect crop diseases after the leaves turn yellow or brown, which is too late to save the harvest. Standard 2D Convolutional Neural Networks (CNNs) cannot process the massive depth of data required for early detection.
